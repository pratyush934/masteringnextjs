import React from "react";

const Form = ({
  type,
  post,
  setPost,
  submitting,
  handleSubmit,
}: {
  type: string;
  post: any;
  setPost: any;
  submitting: any;
  handleSubmit: any;
}) => {
  return <div>Form</div>;
};

export default Form;
